# الدستور العام — Commit Commander

هذا الملف **ملزِم**. أي كود يخالفه يُرفض ولو كان يعمل. الغرض منه أن يبقى المشروع
قابلاً للصيانة بعد ستة أشهر من آخر مرة فتحناه، وأن يمنع الانزلاق التدريجي نحو
ملفات متضخمة ومسؤوليات متداخلة.

القارئ المستهدف: أي إنسان أو نموذج يكتب في هذا الريبو. اقرأه قبل أول سطر.

---

## ١. القرارات المعمارية الملزمة

هذه قرارات مُتخذة ومقفلة. لا يُعاد فتحها إلا بتعديل هذا الملف صراحةً (انظر §٩).

| القرار | البديل المرفوض | لماذا |
|---|---|---|
| TypeScript في كل مكان، `strict: true` | JavaScript، أو `strict: false` | العقد بين الطبقات يجب أن يكون مفحوصاً وقت الترجمة، لا وقت التشغيل |
| monorepo بـ npm workspaces: `apps/api`، `apps/web`، `packages/shared` | ريبوهات منفصلة، أو نسخ الأنواع يدوياً | العقود (DTOs) تُعرَّف مرة واحدة ويستهلكها الطرفان. تغيير حقل يكسر الترجمة في الطرف الآخر فوراً |
| Express + Prisma + PostgreSQL | ORM آخر، أو SQL خام | مقرر. `schema.prisma` هو المصدر الوحيد لشكل البيانات |
| React + Vite + TanStack Query + Axios | CRA، Redux، fetch يدوي في المكونات | حالة الخادم ليست حالة تطبيق. TanStack Query يملك الـ cache والـ retry ولا نعيد كتابتهما |
| CSS عام مبني على tokens | Tailwind، CSS-in-JS، ملف أنماط لكل مكوّن | الهوية البصرية تُعرَّف في مكان واحد ولا تُخترع في المكونات |
| طابور outbox في قاعدة البيانات | معالجة مباشرة، أو Redis/BullMQ | لا تقرير يُفقد بسبب فشل مؤقت، وبدون حاوية ثالثة |
| Docker Compose للتشغيل | تثبيت يدوي | `docker compose up` أمر واحد يشغّل كل شيء |

---

## ٢. قاعدة الاعتماد (Dependency Rule)

الاعتماد يتجه للداخل فقط. الطبقة الداخلية **لا تعرف** بوجود الخارجية.

```
            ┌──────────────────────────────────────┐
            │  transport   routes · controllers    │  ← Express, HTTP, req/res
            ├──────────────────────────────────────┤
            │  application service · use-case      │  ← تنسيق، معاملات
            ├──────────────────────────────────────┤
            │  domain      قواعد، كيانات، حسابات   │  ← دوال نقية، صفر I/O
            └──────────────────────────────────────┘
              ↑ infrastructure: prisma, axios, fs
                (يُحقن للداخل، لا يُستورد من الداخل)
```

| ممنوع منعاً باتاً | لماذا |
|---|---|
| `import` لـ `express` داخل `domain/` أو `modules/*/**.service.ts` | يجعل منطق العمل غير قابل للاختبار بدون خادم |
| `req` أو `res` تُمرَّر لأي service | الـ service يستقبل بيانات، لا طلبات HTTP |
| `PrismaClient` يُستورد داخل `domain/` | الـ domain دوال نقية تُختبر بلا قاعدة بيانات |
| منطق عمل داخل controller | الـ controller يفكّ الطلب، ينادي service واحد، يغلّف الرد. لا شيء غير ذلك |
| استعلام Prisma داخل controller | يتجاوز طبقة الخدمة ويجعل المنطق غير قابل لإعادة الاستخدام |
| `any` في أي مكان | الأنواع هي العقد. `unknown` + تحقق بـ zod هو البديل الصحيح |
| `as unknown as` خارج `apps/api/src/core/json.ts` | أعمدة Json في Prisma لا تصف أشكالنا، فالتضييق يصير في دالتين مسمّاتين (`toJson`/`fromJson`) قابلتين للـ grep — بدل نثر التحويلات في كل مكان حتى تختفي عن النظر |

---

## ٣. النصوص المرئية

**لا يوجد نص مرئي للمستخدم داخل الكود. إطلاقاً.**

كل نص — تسمية في الواجهة، رسالة خطأ من الـ API، اسم مخالفة يُمرَّر للنموذج، سبب
في السجل — يعيش في `packages/shared/src/i18n/` كمفتاح له ترجمة عربية وإنجليزية.
الكود يُصدِر **مفاتيح**، والعرض يحوّل المفتاح إلى نص.

هذا يشمل الـ API: رسائل الخطأ ترجع `{ error: "repo.duplicate" }` لا جملة إنجليزية.

**الاستثناء الوحيد:** رسائل السجلات التقنية (`logger.error`) الموجّهة للمشغّل لا للمستخدم.

---

## ٤. حدود صارمة

| الحد | القيمة | إذا تجاوزته | يحرسه |
|---|---|---|---|
| طول الملف | 200 سطر | قسّمه. ملف 400 سطر يعني مسؤوليتين | `tests/constitution/size.test.ts` |
| طول الدالة | 40 سطر | استخرج دالة مساعدة مسمّاة | eslint `max-lines-per-function` |
| عمق التداخل | 3 مستويات | استخدم early return أو استخرج | eslint `max-depth` |
| معاملات الدالة | 3 | مرّر كائناً مسمّى | eslint `max-params` |
| مكوّن React | 150 سطر | استخرج مكوّناً فرعياً أو hook | `tests/constitution/size.test.ts` |
| `useEffect` في ملف | 1 | أكثر من واحد يعني منطقاً يجب أن يكون hook | `tests/constitution/size.test.ts` |

**تعديل بتاريخ ٢٠٢٦-٠٧-٢٧ (§٩).** كان مكتوباً هنا: «حدود إرشادية تُراجع بالعين، ما
عدا طول الملف». والسبب في إلغاء ذلك قياس لا رأي: عند أوّل فحص آليّ وُجد **٢٠ خرقاً
لحدّ المعاملات و٤ لحدّ التداخل** تراكمت دون أن يوقفها أحد — وهي الحجّة للقاعدة لا
عليها. حدٌّ «يُراجع بالعين» حدٌّ غير مطبَّق، وجدولٌ يسمّي نفسه «صارماً» ثم يُنهي
نفسه بأنه استرشادي يجعل الالتزام مسألة مزاج.

الآن كلّها آلية. والاستثناء الوحيد المسجَّل: `errorHandler` في
[apps/api/src/middleware/error.middleware.ts](apps/api/src/middleware/error.middleware.ts)
يبقى بأربعة معاملات لأن Express يميّز معالج الأخطاء بـ`fn.length === 4` وبلا شيء
آخر — حذف المعامل الرابع لا يقصّر الدالة، بل يُلغي تسجيلها معالجَ أخطاء أصلاً.
الاستثناء مكتوب في مكانه بـ`eslint-disable-next-line` ومعه سببه، لا كتعليق «مؤقتاً».

---

## ٥. اصطلاحات التسمية

| العنصر | النمط | مثال |
|---|---|---|
| مجلدات | `kebab-case` | `violation-rules/` |
| ملفات الباك | `<name>.<role>.ts` | `auth.service.ts`، `repos.controller.ts` |
| مكوّنات React | `PascalCase.tsx` | `RepositoryCard.tsx` |
| hooks | `use<Name>.ts` | `useRepositories.ts` |
| أنواع وواجهات | `PascalCase`، بلا بادئة `I` | `Repository` لا `IRepository` |
| ثوابت | `SCREAMING_SNAKE` | `MAX_RETRY_ATTEMPTS` |
| مفاتيح i18n | `domain.thing.state` | `repos.form.nameInvalid` |
| جداول قاعدة البيانات | `snake_case` جمع | `delivery_attempts` |

**ملف واحد = تصدير رئيسي واحد.** لا تجمع ثلاث خدمات غير مترابطة في ملف.

---

## ٥.١ الاستيراد

- ممنوع `../../../`. استخدم مسارات مستعارة: `@/` للتطبيق، `@shared/` للحزمة المشتركة.
- ممنوع barrel file (`index.ts` يعيد تصدير كل شيء) داخل `modules/` — يخلق دورات اعتماد ويخفي التبعيات الحقيقية. مسموح في `packages/shared` فقط.

---

## ٦. معالجة الأخطاء

| القاعدة | التفصيل |
|---|---|
| أخطاء متوقعة = أصناف | `AppError` وفروعها، تحمل `statusCode` و`i18nKey` |
| لا `try/catch` في controller | `asyncHandler` يلتقط، و`errorMiddleware` يحوّل لرد موحّد |
| لا تبتلع خطأ | `catch {}` فارغ ممنوع. إما تعالج أو تسجّل وترمي |
| التكامل الخارجي لا يرمي | عملاء OpenRouter وDiscord يرجعون `Result` صريحاً، لأن فشلهم متوقع لا استثنائي |
| كل رد خطأ له نفس الشكل | `{ ok: false, error: <i18nKey>, details?: [] }` |

---

## ٧. الأمان — غير قابل للتفاوض

| القاعدة | لماذا |
|---|---|
| توقيع ويبهوك GitHub يُتحقق على **البايتات الخام** قبل أي parse | إعادة تسلسل الكائن تنتج بايتات مختلفة والتوقيع لن يطابق أبداً |
| كل مقارنة أسرار بـ `timingSafeEqual` | `===` يخرج عند أول بايت مختلف ويسرّب معلومة زمنية |
| لا سر يخرج في أي رد API | نرجع "موجود/ناقص" لا القيمة |
| رسائل الكوميت **بيانات معادية** | تُنظّف وتُقصّ وتُغلّف قبل أن تصل النموذج |
| كوكي الجلسة `HttpOnly` + `SameSite=Strict` + `Secure` خارج التطوير | يمنع XSS من سرقتها وCSRF من استخدامها |
| متغيرات البيئة تُتحقق عند الإقلاع بـ zod | الفشل عند البدء أفضل من فشل غامض بعد أسبوع |

---

## ٨. كيف تُضاف ميزة

بهذا الترتيب. لا تبدأ من الواجهة.

1. **العقد** — نوع أو zod schema في `packages/shared/src/contracts/`
2. **المفاتيح** — نصوص i18n بالعربي والإنجليزي في `packages/shared/src/i18n/`
3. **البيانات** — تعديل `schema.prisma` + `prisma migrate dev --name <وصف>`
4. **الـ domain** — دالة نقية إن كان فيها منطق حقيقي، مع اختبارها
5. **الـ service** — التنسيق والوصول لقاعدة البيانات
6. **الـ controller + route** — تحقق بـ zod، نداء واحد، رد موحّد
7. **طبقة api في الفرونت** — دالة في `features/<x>/api/`
8. **الـ hook** — `useQuery` / `useMutation`
9. **المكوّن** — يستهلك الـ hook، بلا `fetch` ولا منطق شبكة
10. **الصفحة** — تركيب مكوّنات فقط

**قاعدة إضافة قاعدة مخالفة جديدة:** ملف واحد في `domain/violations/rules/`،
تسجيله في `registry.ts`، وثلاثة مفاتيح i18n. لا شيء آخر يُلمس.

---

## ٩. تعديل الدستور

هذا الملف يسبق أي تفضيل شخصي وأي "الأسرع الآن". لتغيير قاعدة فيه:

1. عدّل الجدول المعني هنا **في نفس الـ commit** الذي يخالف القاعدة القديمة.
2. اكتب سطر "لماذا" — قاعدة بلا سبب مكتوب تُحذف لاحقاً بلا نقاش.

**ممنوع** إضافة استثناء في الكود بتعليق `// TODO: يخالف الدستور مؤقتاً`.
إما القاعدة صحيحة فالتزم بها، أو خاطئة فعدّلها هنا.

## دساتير فرعية

- [`apps/api/CONSTITUTION.md`](apps/api/CONSTITUTION.md) — بنية الباك إند
- [`apps/web/CONSTITUTION.md`](apps/web/CONSTITUTION.md) — بنية الفرونت والهوية البصرية
