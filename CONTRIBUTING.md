# المساهمة

> Written in Arabic, like the constitutions it summarises. The binding rules live in
> [`CONSTITUTION.md`](CONSTITUTION.md) and the two sub-constitutions; the automated
> guards that enforce them are in [`tests/`](tests/) and their failure messages are
> in English.

---

## قبل أول سطر

ثلاثة ملفات **ملزِمة**، لا استرشادية. كودٌ يخالفها يُرفض ولو كان يعمل:

| الملف | يحكم |
|---|---|
| [CONSTITUTION.md](CONSTITUTION.md) | القرارات المعمارية · اتجاه الاعتماد · الحدود · التسمية · الأخطاء · الأمان |
| [apps/api/CONSTITUTION.md](apps/api/CONSTITUTION.md) | خريطة الطبقات · تشريح الوحدة · قواعد الطابور · سياسة Prisma |
| [apps/web/CONSTITUTION.md](apps/web/CONSTITUTION.md) | البنية بالميزة · قاعدة التوكنز · الاتجاه · ملكية الحالة |

ولماذا يستحقّ المشروع هذا التشدّد على نفسه: [docs/VISION.md](docs/VISION.md).

---

## الأمر الوحيد

```bash
npm install
npm run verify        # typecheck + lint + build + test  ← قبل ادّعاء الإنجاز
```

`verify` **يبني قبل أن يختبر**، عمداً: ميزانية وزن الصفحة تُقاس على المخرجات
المبنيّة، وحارس لا يجد ما يقيسه لا يجوز أن يمرّ صامتاً.

| الأمر | متى |
|---|---|
| `npm run dev` | التطوير: الطرفان يعيدان التحميل، وPostgres على ٥٤٣٣ |
| `npm run test:guards` | الحرّاس وحدهم — أسرع حلقة بعد تعديل بنيوي |
| `npm run build -w @commander/shared` | **بعد أي تعديل على `packages/shared`** وقبل فحص أنواع أيّ تطبيق: التطبيقان يستوردان `dist` |
| `npm run logs` · `npm run down` | تشغيل |

Prisma: عدّل `apps/api/prisma/schema.prisma` ثم
`npm run prisma:dev -w @commander/api -- --name <وصف>`. لا `prisma db push` خارج
التجربة المحليّة.

---

## ترتيب إضافة ميزة

بهذا الترتيب. **لا تبدأ من الواجهة** — البدء منها ينتج عقداً مُخترعاً في المكوّن ثم
خادماً يُجبَر على مجاراته.

| # | الطبقة | أين |
|---|---|---|
| ١ | العقد | `packages/shared/src/contracts/` |
| ٢ | المفاتيح — عربي وإنجليزي | `packages/shared/src/i18n/` |
| ٣ | البيانات + هجرة | `apps/api/prisma/schema.prisma` |
| ٤ | الـdomain — دالة نقية مع اختبارها | `domain/` |
| ٥ | الـservice — التنسيق وقاعدة البيانات | `modules/<x>/<x>.service.ts` |
| ٦ | الـcontroller + route | تحقّق بـzod، **نداء واحد**، ردّ موحّد |
| ٧–٩ | api ← hook ← مكوّن | `features/<x>/` |
| ١٠ | الصفحة | تركيب فقط |

**قاعدة مخالفة جديدة:** ملف في `domain/violations/rules/`، سطر في `registry.ts`،
وثلاثة مفاتيح i18n. لا شيء آخر يُلمس — ونسيان السجلّ خطأ ترجمة بالتصميم.

---

## القواعد التي تُكسر بحسن نيّة

هذه ليست كل الدستور، بل ما يخالفه الناس دون قصد. كلّها **محروسة آلياً** — ستعرف
قبل أن تفتح PR.

| القاعدة | لماذا ليست تفضيلاً |
|---|---|
| **لا نصّ مرئي في الكود** | كل تسمية وخطأ واسم مخالفة مفتاحٌ في `i18n/`. القاموس العربي هو المرجع، ومجموعة مفاتيحه **هي** نوع `TranslationKey` — فمفتاح ناقص من الإنجليزي خطأُ ترجمة، لا مفاجأة وقت تشغيل. الاستثناء الوحيد: `logger.*` فهو للمشغّل |
| **لا لون ولا خط ولا px خارج `tokens.css`** | المكوّنات تستهلك دلالات `--color-*`. الوصول إلى بدائيّة `--palette-*` يكسر الوضع الفاتح — وهو سبب وجود الطبقتين |
| **لا `[dir="rtl"]`** | خصائص منطقية (`margin-inline-start`, `inset-block-end`, `text-align: start`). ورقة أنماط واحدة تخدم الاتجاهين |
| **لا `any`، ولا `as unknown as` خارج `core/json.ts`** | أعمدة Json في Prisma لا تصف أشكالنا، فالتضييق محصورٌ في دالتين مسمّاتين قابلتين للـgrep |
| **الـcontroller ينادي service واحداً** | بلا استعلام Prisma، بلا تحقّق، بلا `try/catch`. `asyncHandler` + `errorHandler` يملكان مسار الخطأ |
| **`domain/` نقيّ** | لا Express، لا Prisma، لا logger. حين يحتاج محرّك القواعد أن يبلّغ عن قاعدة رمت، يأخذ `onRuleError` محقونة |
| **٢٠٠ سطر للملف، ١٥٠ للمكوّن، ٤٠ للدالة، ٣ للتداخل، ٣ للمعاملات، `useEffect` واحد** | قسّم بالمسؤولية. **لا تُرفع الحدود** — وكل صفّ في جدول §٤ يسمّي حارسه |

---

## تعديل قاعدة

القواعد ليست مقدّسة، لكنها ليست اختيارية. لتغيير واحدة:

١. عدّل الجدول المعني في الدستور **في نفس الـcommit** الذي يخالف القاعدة القديمة.
٢. اكتب سطر «لماذا». قاعدةٌ بلا سبب مكتوب تُحذف لاحقاً بلا نقاش.

**ممنوع** `// TODO: يخالف الدستور مؤقتاً`. إمّا القاعدة صحيحة فالتزم، أو خاطئة فعدّلها.

قيدٌ خارجيّ حقيقي يأخذ `eslint-disable` **ومعه سببه** في مكانه. الاستثناء الوحيد
المسجَّل اليوم: `errorHandler` يبقى بأربعة معاملات لأن Express يميّز معالج الأخطاء
بـ`fn.length === 4` وبلا شيء آخر — حذف الرابع لا يقصّر الدالة، بل يُلغي تسجيلها
معالجَ أخطاء أصلاً.

---

## إضافة حارس

كل ميزة تُضاف تُضاف معها حراستها. التفصيل في [tests/README.md](tests/README.md)، والقواعد الخمس:

١. الرقم — إن وُجد — يذهب إلى `tests/lib/budgets.ts`، لا داخل الاختبار.
٢. الحارس يجمع **كل** المخالفات ويفشل مرّة واحدة بقائمة.
٣. رسالة الفشل تقول **ماذا يُفعل**، لا ماذا حدث.
٤. الاستثناء نمطٌ مسمّى مع سبب مكتوب.
٥. **صفر إنذارات كاذبة.** حارسٌ يُطلق على شغل سليم يُطفأ — والمطفأ أسوأ من الغائب.

والسؤال الذي يُطرح على كل قاعدة مقترحة: **أي حارس يفشل لو كُسرت؟** إن كان الجواب
«المراجعة»، فاكتب الحارس أو عدّل القاعدة — ولا تترك الوثيقة غامضة.

---

## أين تضع ما عندك

| ما عندك | مكانه |
|---|---|
| فكرة أو اقتراح | [`docs/proposals/`](docs/proposals/) — ملفّ من القالب، وPR بالملفّ وحده بلا كود |
| عطل | PR مباشرة، ومعه اختبار يفشل قبل الإصلاح |
| ملاحظة واجهة | صفّ في [`docs/UI-AUDIT.md`](docs/UI-AUDIT.md) **وقت مقابلتها**، لا لاحقاً من الذاكرة |
| سؤال «لماذا بُني هكذا؟» | سجلّات التصميم في [`docs/`](docs/README.md) — والغالب أن الجواب مكتوب |
